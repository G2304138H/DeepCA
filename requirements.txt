# Install the CUDA-enabled PyTorch pair separately from the official cu124
# index before installing this file.  See docs/IMAGECAS_ADAPTATION.md.
# These pins target the server's Python 3.12.10 while avoiding the larger
# NumPy 2 behavior/ABI transition.
numpy==1.26.4
PyYAML==6.0.1
scipy==1.12.0
scikit-image==0.22.0

# Pinned scikit-image runtime dependencies make Python 3.12 installs
# reproducible across the training and evaluation hosts.
imageio==2.34.0
lazy_loader==0.3
networkx==3.2.1
packaging==24.0
Pillow==10.2.0
PyWavelets==1.5.0
tifffile==2023.12.9
